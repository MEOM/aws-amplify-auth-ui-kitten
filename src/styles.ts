import {StyleSheet} from 'react-native'

export const AuthStyles = StyleSheet.create({
  input: {
    maxWidth: 300,
    marginTop: 16,
  },
  button: {
    marginTop: 16,
    marginHorizontal: 8
  },
  header: {
    margin: 16,
    maxWidth: 320,
    textAlign: "center"
  }
})

